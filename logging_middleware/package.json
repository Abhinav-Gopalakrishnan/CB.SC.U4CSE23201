{
  "name": "logging-middleware",
  "version": "1.0.0",
  "description": "Reusable logging middleware that sends structured log entries to the Afford evaluation service API",
  "main": "index.js",
  "scripts": {
    "test": "node test.js"
  },
  "dependencies": {
    "axios": "^1.7.0"
  }
}
